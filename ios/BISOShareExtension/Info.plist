<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Add to BISO</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Only the types the reimbursement API accepts. The generic
			     file/image rules offered BISO for HEIC and WebP photos it
			     could never submit, so the share sheet said "Receipt added"
			     for a receipt that was then dropped. A HEIC photo from
			     Photos still matches, because Photos also offers it as
			     public.jpeg and iOS converts it on load.

			     The counts keep what the old rule allowed: any number of
			     shared items, as long as one of them holds a receipt BISO
			     can take, and at most 20 attachments in all (the old
			     SupportsFile/ImageWithMaxCount of 20). The extension says
			     which attachments it could not add. -->
			<key>NSExtensionActivationRule</key>
			<string>SUBQUERY (
				extensionItems,
				$extensionItem,
				SUBQUERY (
					$extensionItem.attachments,
					$attachment,
					ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "com.adobe.pdf"
					|| ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.jpeg"
					|| ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.png"
				).@count &gt;= 1
			).@count &gt;= 1
			AND extensionItems.@sum.attachments.@count &lt;= 20</string>
		</dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
	</dict>
</dict>
</plist>
